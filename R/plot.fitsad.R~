plot.fitsad <- function(x,ask=FALSE,which=c("octaves","rad"), ...){
  y <- x$data$x
  oct.df <- octav(y)
  
  mod.names <- names(x)
  if (ask) {
        oask <- devAskNewPage(TRUE)
        on.exit(devAskNewPage(oask))
    }
  else par(mfrow=1,2)
  plot(oct.df,...)
